name: Daily Data Refresh

on:
  schedule:
    # Runs at 00:00 UTC every day
    - cron: '0 0 * * *'
  workflow_dispatch: # Allows you to trigger it manually from GitHub Actions tab

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run refresh script
        env:
          # This line fixes the "ModuleNotFoundError: No module named 'app'"
          PYTHONPATH: ${{ github.workspace }}
        run: |
          python jobs/refresh_data.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          # Stage all data changes, including the new futures curves
          git add data/processed/*.csv
          git add data/processed/futures_curves/*.csv
          git commit -m "auto: daily data refresh" || echo "No changes to commit"
          git push